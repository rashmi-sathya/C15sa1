

var car={
 //Mention the car properties in key value pair
 
};
function setup() {
  createCanvas(400, 400);
 //fill the object need to display
  console.log(car.);
  
}

function draw() {
  background(220);
}
